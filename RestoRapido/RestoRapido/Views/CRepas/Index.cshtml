@model IEnumerable<RestoRapido.Models.CRepas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.mRepNom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.mRepNom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.mRepID }) |
            @Html.ActionLink("Details", "Details", new { id=item.mRepID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.mRepID })
        </td>
    </tr>
}

</table>
