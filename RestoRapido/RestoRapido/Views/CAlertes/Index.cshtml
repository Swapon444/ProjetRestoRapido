@model IEnumerable<RestoRapido.Models.CAlerte>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AlerteClientID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AlerteServeurID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AlerteClientID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AlerteServeurID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AlerteID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AlerteID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AlerteID })
        </td>
    </tr>
}

</table>
