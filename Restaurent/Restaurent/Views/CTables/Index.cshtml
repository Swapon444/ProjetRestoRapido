@model IEnumerable<Restaurent.Models.CTable>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.i_TableNum)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.i_RestaurantID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.i_TableNum)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.i_RestaurantID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.i_TableID }) |
            @Html.ActionLink("Details", "Details", new { id=item.i_TableID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.i_TableID })
        </td>
    </tr>
}

</table>
